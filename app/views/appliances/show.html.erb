<div class="container my-5" >
  <div>
    <div class="row border shadow-sm rounded p-5">
      <div class="col-5">
        <%= image_tag "https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg", class: "img-fluid rounded"%>
      </div>

      <div class="col d-flex flex-column">
        <h2 class="fw-bolder"><%= @appliance.name %></h2>
        <p class="fw-medium fs-5 mb-5"><%= @appliance.category %></p>
        <p class="fw-light"><%= @appliance.description %></p>

          <div class="mt-4">
            <p>For <%= @appliance.eaters %> persons</p>
            <p>Condition : <%= @appliance.condition %></p>
          </div>

        <div class="d-flex justify-content-between mt-auto">
          <p class="fw-bolder fs-5"><%= @appliance.price %> €</p>
          <%= link_to 'Book it!', new_appliance_booking_path(@appliance), class: "btn btn-custom align-self-end" %>
        </div>
      </div>
    </div>

  </div>
</div>
